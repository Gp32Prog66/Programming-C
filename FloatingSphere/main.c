#include <stdio.h>
#include <stdlib.h>

int main()
{

    int radius, weight;
    printf("Enter the radius of the sphere");
    scanf("%d", &radius);
    printf("Enter the weight of the sphere");
    scanf("%d", &weight);
    return 0;
}
